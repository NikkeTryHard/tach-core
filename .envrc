# Project-specific environment for tach-core
# This overrides any Windows pyenv paths inherited via WSL

# Use the project's virtual environment Python for PyO3
export PYO3_PYTHON="$(pwd)/.venv/bin/python"

# Activate virtual environment if it exists
if [ -f .venv/bin/activate ]; then
    source .venv/bin/activate
fi

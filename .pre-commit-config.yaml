# Pre-commit configuration for tach-core
# Install: pip install pre-commit && pre-commit install && pre-commit install --hook-type pre-push
# Run manually: pre-commit run --all-files
# Docs: https://pre-commit.com

repos:
  # Standard pre-commit hooks
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
        exclude: ^(target/|coverage/)
        stages: [pre-commit] # Only on commit, not push (auto-fixes files)
      - id: end-of-file-fixer
        exclude: ^(target/|coverage/)
        stages: [pre-commit] # Only on commit, not push (auto-fixes files)
      - id: check-yaml
      - id: check-toml
      - id: check-merge-conflict
      - id: check-added-large-files
        args: ["--maxkb=500"]

  # Rust hooks (local - uses system cargo)
  - repo: local
    hooks:
      - id: cargo-fmt
        name: cargo fmt
        description: Format Rust code with rustfmt
        entry: cargo fmt --all --
        language: system
        types: [rust]
        pass_filenames: false
        stages: [pre-commit] # Only on commit, not push (auto-fixes files)

      - id: cargo-clippy
        name: cargo clippy
        description: Lint Rust code with clippy (warnings as errors)
        entry: bash -c 'cargo clippy --all-targets --all-features -- -D warnings'
        language: system
        types: [rust]
        pass_filenames: false

      - id: cargo-test
        name: cargo test (lib)
        description: Run library unit tests
        entry: cargo test --lib
        language: system
        types: [rust]
        pass_filenames: false
        stages: [pre-commit]

      - id: cargo-test-full
        name: cargo test (full)
        description: Run full test suite including integration tests
        entry: bash -c 'cargo test --lib && cargo test --test "*"'
        language: system
        pass_filenames: false
        stages: [pre-push]

      - id: build-docs
        name: build full documentation
        description: Regenerate FULL_DOCUMENTATION.md from source docs
        entry: ./scripts/build-docs.sh
        language: script
        files: ^docs/.*\.md$
        pass_filenames: false
        stages: [pre-commit] # Only on commit, not push (auto-fixes files)

  # Python hooks (for gauntlet tests)
  - repo: https://github.com/psf/black
    rev: 24.10.0
    hooks:
      - id: black
        language_version: python3
        files: ^tests/
        stages: [pre-commit] # Only on commit, not push (auto-fixes files)

  - repo: https://github.com/PyCQA/isort
    rev: 5.13.2
    hooks:
      - id: isort
        files: ^tests/
        stages: [pre-commit] # Only on commit, not push (auto-fixes files)

# CI configuration
ci:
  autofix_commit_msg: |
    [pre-commit.ci] auto fixes from pre-commit hooks

    for more information, see https://pre-commit.ci
  autofix_prs: true
  autoupdate_branch: ""
  autoupdate_commit_msg: "[pre-commit.ci] pre-commit autoupdate"
  autoupdate_schedule: weekly
  skip: [cargo-fmt, cargo-clippy, cargo-test, cargo-test-full, build-docs] # Skip Rust hooks in CI (handled locally)
  submodules: false

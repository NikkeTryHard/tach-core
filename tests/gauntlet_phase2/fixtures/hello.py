# fixtures/hello.py - Test module for E2E loader verification
# This module is compiled and loaded via the Zero-Copy Loader


def greet(name):
    """Say hello to someone."""
    return f"Hello, {name}!"


VALUE = 42

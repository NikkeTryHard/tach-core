[tool.pytest_env]
TACH_TEST_VAR = "hello_from_pyproject"
TACH_DEBUG_MODE = "true"
TACH_PHASE3_VERIFIED = "true"

[tool.pytest.ini_options]
markers = [
    "allow_threads: marks test as allowed to spawn threads that outlive execution (tach-core 0.1.2)",
]
